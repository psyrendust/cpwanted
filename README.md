# cpwanted

Manage wanted items in cp.

## Usage

Create a config file in your home folder

```shell
mkdir ~/.cpwanted
touch ~/.cpwanted/config.yaml
```

Add the following to your config:

```yaml
url: localhost
port: 5050
```

### Development

```shell
npm run dev
```

### Build

```shell
npm run build
```
